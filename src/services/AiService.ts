/**
 * Responsible strictly for semantic mapping via Gemini 2.5 Pro.
 * Does not handle validation, queueing, or business logic.
 */
export class AiService {
  public static async mapToCrmSchema(csvBatch: any[]) {
    throw new Error('Not implemented: AiService.mapToCrmSchema');
  }
}
